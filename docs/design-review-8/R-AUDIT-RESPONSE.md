# External targeted audit — R-AUDIT Response

> **Audited:** S4-POST-R1 reconciliation ledger + S4-POST-R2 exact-tree two-parent candidate
> **Refs:** candidate `2ed736b` on `origin/codex/s4-post-r2-reconcile`; evidence `a2500a4` on
> `origin/codex/s4-03-cycle1`; authoritative refs unmoved
> **Method:** every identity regenerated by the reviewer from raw Git objects after
> `git fetch --all`; nothing trusted from the request or the ledger until Git reproduced it.
> Left uncommitted per the charter.

## 1. Verdict

**READY.** The candidate may advance host `main` by fast-forward and proceed to the separately
controlled deployment-branch cutover slice under the frozen §5 choreography.

**This response authorizes nothing else** — no legacy retirement, no branch deletion, no
side-effecting-lane migration (the hard-kill lease-expiry drill gate stands), and no Stage 5. The
cutover itself remains its own controlled slice with its own live verification, and independent
acceptance is still required before any retirement work.

## 2. Independently derived identities

- Candidate commit `2ed736b6bfadff4b4ba1a1384224b32e03d5517c`; parents exactly
  `9348f85…` (first) then `7df6b7f…` (second), in that order; trailer present.
- Candidate tree = accepted-evidence tree = `ded6d43ace2fced88600f19128dedcfcfe9fe0be`;
  `git diff 9348f85 2ed736b --name-only` is **empty** (zero paths — content, mode, symlink, and
  deletion drift all excluded by tree-hash identity plus the explicit empty recursive diff).
- Both accepted histories and current `origin/main` are ancestors; `git merge-base
  --is-ancestor 7df6b7f 2ed736b` succeeds → the `main` advance is a true `--ff-only` move.
- One-sided counts regenerated: **27** evidence-only (`7df6b7f..9348f85`) and **3** main-only
  (`9348f85..7df6b7f`) — exactly the ledger's claim.
- All four ledger checksums recomputed by the reviewer and **matched bit-for-bit**: both reverse
  topological commit-id lists and both `a0019cd`-based name/status manifests.
- Tags: all three exist locally and remotely as **annotated tag objects** (object ids
  `694da1c…`, `28d6d8d…`, `75994c6…`) peeling to exactly `7df6b7f`, `3f50b7d`, and `9348f85`
  respectively — matching the R2 evidence table. No branch was archived or deleted.
- `origin/main` = `7df6b7f`; `origin/staging-prep` = `3f50b7d`; the candidate exists only on the
  non-deploying `codex/` branch; `a2500a4` is not a candidate parent and contributed nothing to
  the candidate tree (evidence docs only, above `9348f85`).

## 3. Audit A — ledger regeneration

All 27 production/evidence-only rows are `retain` and correct by construction (the candidate tree
IS the accepted production tree; each row's named oracle is part of the already-accepted Stage-4
gate set). The three default-only rows were each verified against source, not messages:

- `ef084ab` **superseded — verified.** Its unique artifact (the a1 pin + first preflight shape)
  is intentionally absent: the candidate pins the accepted immutable a2 URL/hash (regressing to
  this patch would reinstall a1 over the accepted authorization fix). Its router-safe test intent
  exists on the candidate (both rewritten tests present and asserting via the application OpenAPI
  oracle — reviewer-grepped at `2ed736b`).
- `90fa63d` **present — proven, not asserted.** `git patch-id` computed by the reviewer:
  `2583d01c…` for both `90fa63d` and production `53f674b` — identical patches.
- `7df6b7f` **superseded — verified.** The integration was carried to the production line
  byte-identically at reconciliation (established during the cycle-1 acceptance), then hardened
  by the accepted fix chain through `3f50b7d`; the candidate tree passes the complete 72-test
  gate including the offline Alembic compile, and its Alembic environment is the production
  line's working one (production ran `alembic current` at head during Stage 4).

**Zero forward ports confirmed:** no main-only behavior is missing from the candidate tree that
any oracle demands — the only main-only artifacts are the regressive a1 pin (correctly absent),
the patch-equal evidence doc (present), and the stale-shape integration (superseded by the
accepted production implementation). No default-only security or migration fix exists outside
those three commits. The R8-01 manifest binding in the ledger (base `9348f85`, empty forward-port
list, expected tree `ded6d43…`, zero allowed differing paths) is exactly satisfied.

## 4. Audit E — gates and byte-identical-tree reasoning

Reproduced by the reviewer on 2026-07-20 at the evidence checkout (whose tree differs from the
candidate tree only by the two R1/R2 evidence documents — gate-irrelevant paths): `uv lock
--check` resolves clean; host suite **72 passed / 5 existing infrastructure skips**; Ruff clean;
MyPy clean across 64 source files. The offline Alembic full-upgrade compile and the three-way
import checks run inside that suite. taskq suite previously reproduced at the same library tip:
450 passed / 1 opt-in skip with a CI-shaped Redis, Ruff clean. Dependency pins verified:
OutLabs Auth exactly `0.1.0a24`; taskq exactly the immutable a2 release URL + SHA-256 (wheel hash
previously recomputed by this reviewer from the release asset).

**Production-shape harness decision (explicit, as the charter requires):** the immutable-tree
proof plus the accepted Stage-4 harness evidence **is sufficient** for this ancestry-only
candidate. The harness exercises tree content against a database; both are unchanged by a commit
whose tree is byte-identical to the tree that produced the accepted evidence. The residual risk
is deployment-level, not tree-level, and the frozen cutover slice already requires live
health/auth/canonical-path/legacy-probe verification plus a full rolling deployment before the
move is complete (§5.3–5.5). No rerun precondition is imposed.

**Unconfigured-import check:** the R2 note that a bare `app.main` import fails without required
configuration is the accepted fail-closed boundary, not drift — `DOCS_USERNAME`/`DOCS_PASSWORD`
are required settings with no defaults at the candidate tree (config.py:207-208), a posture that
predates Stage 4. The configured import passing is the correct complement.

**Images:** the API and worker image digests are recorded in the R2 evidence; the reviewer did
not rebuild them (environmental limit). This is acceptable because §5.3 of the frozen
specification independently requires the built source revision and image tree to match the
accepted candidate **before traffic** — the binding check happens at the only moment it matters.

## 5. Finding registry

**R8A-01 · LOW · Deployment-platform and repository-policy state are not safely inspectable from
this audit.** The GitHub plan does not expose branch-protection status (API 403 —
feature-unavailable, which also means no protection or auto-merge rule can exist today), and the
Coolify tracking state was not probed. Impact: the assurances "nothing can merge/deploy the
candidate during the audit" rest on the recorded facts that Coolify tracks `staging-prep` and the
candidate lives on an untracked `codex/` branch. Smallest remediation (already consistent with
the frozen spec): execute §5.1's pre-move recheck immediately before moving any ref — re-verify
`origin/main`, `origin/staging-prep`, the Coolify branch/revision, and live health — and record
those values in the cutover evidence. Post-cutover, consider enabling default-branch protection
when the plan allows. Owner: the cutover slice.

No other findings. The ledger schema, checksums, dispositions, manifest, tag discipline, and
candidate construction survived every falsification attempt in the charter.

## 6. Contract questions

**None.** Reconciliation requires no taskq SQL, wire, permission, role, or capability change; the
candidate tree contains the already-accepted library artifact pins only.

## 7. BR-01..05 and R8-01 dispositions at this pre-move boundary

- **BR-01 PASS** — ledger independently regenerated: counts, all four checksums bit-identical,
  every default-only disposition source-verified (one by patch-id, two by artifact/gate
  evidence).
- **BR-02 PASS** — exact-tree manifest satisfied: candidate tree ≡ `ded6d43…`, empty recursive
  diff, zero forward ports, zero unclassified paths.
- **BR-03 PASS** — both histories are ancestors; the `main` advance is fast-forward-only;
  nothing was force-moved; trailer present.
- **BR-04 PASS** — lock, suite, Ruff, MyPy, offline Alembic, and import checks reproduced green;
  image builds recorded with the §5.3 pre-traffic identity check binding.
- **BR-05 PASS** — the candidate introduces no taskq SQL/migration/Tier-0/IAM/capability delta
  (tree-identical to the accepted evidence tree).
- **R8-01 PASS** — the ledger froze the manifest construction exactly as bound by the round-8
  response, and the reviewer recomputed it independently.

BR-06..BR-10 belong to the cutover slice and remain open by design.

## 8. Preconditions for moving `main` or deployment branches

**To fast-forward `main`: none.** Proceed under §4's final step (ff-only, no force), then prove
`main` and candidate commit/tree identity.

**To change deployment branches:** only the frozen §5 choreography, with R8A-01's pre-move
recheck executed and recorded at §5.1 (refs, Coolify branch/revision, health, depths, migration
head, ceiling) — no new requirements beyond the specification.

Gates run: `git fetch --all` + raw-object derivations (parents, trees, ancestry, tags, counts,
checksums, patch-ids) in both repos; `uv lock --check`; host suite/Ruff/MyPy at the evidence
checkout; prior same-tree taskq suite and wheel-hash evidence referenced. Environmental limits:
images not rebuilt locally; platform policy state not inspectable (both addressed above). No
production probe was made during this audit; no ref, file (beyond this response), or deployment
state was touched.
